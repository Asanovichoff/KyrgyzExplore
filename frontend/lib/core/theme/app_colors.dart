import 'package:flutter/material.dart';

// Brand palette — use these via Theme.of(context), not directly in widgets.
// Only reference these constants in app_theme.dart.
const kNavy  = Color(0xFF1B3A6B);
const kTeal  = Color(0xFF00898A);
const kDark  = Color(0xFF1A1A2E);
const kLight = Color(0xFFEAF4F4);

const kError   = Color(0xFFD32F2F);
const kSuccess = Color(0xFF2E7D32);
const kGrey    = Color(0xFF757575);
